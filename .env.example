# ---------------------------------------------------------------------------
# SAKAY / SafeTrike environment configuration
# Copy this file to ".env" and paste in the values from your Supabase project:
#   Supabase dashboard  ->  Project Settings  ->  API
# The ".env" file is git-ignored - never commit real keys.
# ---------------------------------------------------------------------------

# Public values (safe to ship in the browser bundle).
VITE_SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
